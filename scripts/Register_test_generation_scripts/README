Procedure to generate test case using python script

1. Load the frio.
2. source FPGA.env
3. change directory to regsiter test generation python script
4. Run the script
5. for smoke test test will be added in $PROJECT_HOME/tests/reg_access/smoke_test directory and for full test it will be added in $PROJECT_HOME/tests/reg_access/register_default
   and $PROJECT_HOME/tests/reg_access/write_read directories

